# chandniTravel — Full Stack
## Quickstart

1. Install MongoDB and ensure it is running.
2. Setup backend:
   - `cd backend`
   - `npm install`
   - `npm run dev`
3. Setup frontend:
   - `cd frontend`
   - `npm install`
   - `npm run dev`
